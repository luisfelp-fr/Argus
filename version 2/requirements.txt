# --- Núcleo: instalação garantida no Streamlit Community Cloud ---
streamlit>=1.33
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
plotly>=5.18
openpyxl>=3.1

# --- Opcionais (o app funciona sem eles: usa RandomForest + importância do modelo) ---
# xgboost normalmente instala bem; descomente para habilitar o modelo XGBoost:
# xgboost>=2.0
# shap depende de numba/llvmlite e pode falhar no build em Python 3.13.
# Para usar: no deploy escolha Python 3.12 (Advanced settings) e descomente:
# shap>=0.44
